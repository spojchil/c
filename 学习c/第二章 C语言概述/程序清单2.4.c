/* nogood.c -- 有错误的程序 */
#include <stdio.h>
int main(void)
( 
    int n, int n2 , int n3;

    /* 该程序有多处错误
    n = 5;
    n2 = n * n;
    n3 = n2 *n2;
    printf("n = %d,n squared = %d, n cubed = %d\n", n, n2, n3)

    return 0;
)
错误分析​​：
​​括号不匹配​​：
错误：main() 使用了圆括号 ( 和 )，但C语言要求函数体必须用​​花括号 {}​​。
修正：将 ( 改为 {，) 改为 }。
​​变量声明错误​​：
错误：int n, int n2, int n3;
C语言中，同类型的多个变量应​​共用1个类型声明​​，不能重复写 int。
修正：改为 int n, n2, n3;。
​​数学逻辑错误​​：
错误：n3 = n2 * n2;
这会计算 n² * n² = n⁴，但注释和输出表明需要的是 n³（即 n * n * n）。
修正：改为 n3 = n2 * n; 或直接 n3 = n * n * n;。
​​缺少分号​​：
错误：printf(...) 语句末尾缺少分号 ;。
​​注释未闭合​​（原代码中）：
错误：多行注释未正确闭合 /* ... */ 